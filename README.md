To install something on Linux

```
sudo apt install ./filname
```

or
```
sudo dpkg -i filename
sudo apt-get install -f
```


### Git pull issue

```
git pull --rebase origin main
```

and then
```
git push oringin branch-name
```
